// Ejemplo 2: forEach para sumar elementos de una lista
let suma = 0;
const numbers = [1, 2, 3, 4, 5];

numbers.forEach(n => suma += n)

console.log("Ejemplo 2: Calculo de la suma de los valores de la lista")
console.log(suma)